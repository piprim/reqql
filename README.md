# ReqQL

*ReqQL* is a generic-driven bridge that seamlessly turns your `HTTP/gRPC` request structs into dynamic `SQL` queries.
Stop maintaining bulky `GraphQL` schemas and let your Go structs define your queries.

